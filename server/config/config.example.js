const config = {
  port: 3000,

  database: {
    DATABASE: 'yourdatabase',
    USERNAME: 'youraccount',
    PASSWORD: 'yourpassword',
    PORT: 'yourport',
    HOST: 'localhost'
  }
}

module.exports = config
